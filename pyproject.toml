[project]
name = "frayune-backend"
version = "0.4.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests/backend"]
pythonpath = ["apps/api", "."]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
